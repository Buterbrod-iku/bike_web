import React from 'react';
import s from './CSS-moduls/discover.module.css';

const Discover = () => {
    return(
        <div className={s.main}>
            <button className={s.but}>
                DISCOVER THE SHOP
            </button>
        </div>
    );
}

export default Discover;